export interface Skill {
  name: string;
  image: string;
}

export interface Project {
  category: string;
  title: string;
  description: string;
  imageURL: string;
  link: string;
  images: string[];
  technologies: string[];
}
